"use strict";
const profileForm=document.getElementById('profileForm');
const passwordForm=document.getElementById('passwordForm');
const notice=document.getElementById('notice');
function message(text,error=false){notice.textContent=text;notice.className=error?'error':'success';}
async function request(url,options){const response=await fetch(url,{credentials:'same-origin',cache:'no-store',...options});let data={};try{data=await response.json();}catch{}if(response.status===401){location.replace('/patient-login.html');throw Error('Please log in again.');}if(!response.ok)throw Error(data.error||'Unable to complete request.');return data;}
(async()=>{try{const patient=await request('/api/patient/me');profileForm.elements.fullName.value=patient.full_name;profileForm.elements.username.value=patient.username;}catch(error){message(error.message,true);}})();
profileForm.addEventListener('submit',async event=>{event.preventDefault();const button=profileForm.querySelector('button');button.disabled=true;try{const fullName=profileForm.elements.fullName.value.trim();await request('/api/patient/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({fullName})});message('Profile saved successfully.');}catch(error){message(error.message,true);}finally{button.disabled=false;}});
passwordForm.addEventListener('submit',async event=>{event.preventDefault();const button=passwordForm.querySelector('button');const currentPassword=passwordForm.elements.currentPassword.value,newPassword=passwordForm.elements.newPassword.value,confirmPassword=passwordForm.elements.confirmPassword.value;if(newPassword!==confirmPassword){message('New passwords do not match.',true);return;}button.disabled=true;try{await request('/api/patient/change-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({currentPassword,newPassword})});passwordForm.reset();message('Password changed successfully.');}catch(error){message(error.message,true);}finally{button.disabled=false;}});
